{
  "name": "@comment-io/claude-code-plugin",
  "version": "1.0.82",
  "private": true,
  "description": "Comment.io skills and CLI notification checks for Claude Code",
  "scripts": {
    "build": "echo \"No runtime build needed; this plugin currently ships Claude skills only.\""
  }
}
