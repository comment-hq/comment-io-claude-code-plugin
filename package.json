{
  "name": "@comment-io/claude-code-plugin",
  "version": "1.0.104",
  "private": true,
  "description": "Comment.io skills, botlet workspaces, and CLI notification checks for Claude Code",
  "scripts": {
    "build": "echo \"No runtime build needed; this plugin ships Claude Code skills and hooks only.\""
  }
}
