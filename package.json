{
  "name": "@comment-io/claude-code-plugin",
  "version": "1.0.10",
  "private": true,
  "description": "Comment.io plugin for Claude Code",
  "scripts": {
    "build": "esbuild comment-io.ts --bundle --platform=node --format=cjs --outfile=comment-io.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "esbuild": "^0.25.0"
  }
}
